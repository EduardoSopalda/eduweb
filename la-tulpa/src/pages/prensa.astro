---
import BaseLayout from '../layouts/BaseLayout.astro';
import { t } from '../i18n';

const locale = 'es';
---

<BaseLayout title={t(locale, 'press.title')} locale={locale}>
  <section class="section page-header">
    <div class="container">
      <span class="accent-line" aria-hidden="true"></span>
      <h1>{t(locale, 'press.heading')}</h1>
    </div>
  </section>

  <section class="section container">
    <div class="content-width">
      <article class="press-item reveal">
        <blockquote>
          <p>&laquo;La estética puede resultar siniestra, pero estos seres son inofensivos.&raquo;</p>
        </blockquote>
        <p class="press-source">
          Reseña de <cite>Ensayo sobre nadie</cite>, con motivo de su estreno en la XVI edición del Festival Escena Mobile.
        </p>
        <aside class="placeholder-content">
          Autor y medio de la reseña pendientes de confirmar para acreditar correctamente. Edu facilitará el texto completo.
        </aside>
      </article>

      <div class="placeholder-content reveal" style="margin-top: var(--space-lg);">
        Reseñas y notas de prensa adicionales pendientes de recibir.
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-header {
    padding-top: calc(4rem + var(--space-xl));
  }

  .press-item {
    padding: var(--space-lg) 0;
    border-bottom: 1px solid var(--color-border);
  }

  blockquote {
    font-family: var(--font-display);
    font-size: clamp(1.3rem, 2vw, 1.8rem);
    font-style: italic;
    color: var(--color-text);
    line-height: 1.5;
    padding-left: var(--space-md);
    border-left: 2px solid var(--color-accent);
  }

  .press-source {
    margin-top: var(--space-sm);
    color: var(--color-text-secondary);
    font-size: 0.95rem;
  }

  .press-source cite {
    font-style: italic;
    color: var(--color-text);
  }

  .press-item .placeholder-content {
    margin-top: var(--space-sm);
  }
</style>
