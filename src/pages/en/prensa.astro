---
import Base from '../../layouts/Base.astro';
import Hero from '../../components/Hero.astro';
---

<Base
  title="Press"
  description="Reviews and press coverage of Manuel Monrò and La Tulpa."
>
  <Hero title="Press" height="section" />

  <section class="section container">
    <div class="prose reveal">
      <p>
        Selected excerpts from reviews and media coverage of Manuel Monrò and La Tulpa's work.
      </p>
    </div>

    <div class="press-list">
      <article class="press-item reveal reveal-delay-1">
        <blockquote>
          <p>
            &laquo;The aesthetic may seem sinister, but these beings are harmless.&raquo;
          </p>
        </blockquote>
        <p class="press-source marginalia">
          Review of <em>Ensayo sobre nadie</em>, following its premiere at the 16th Escena Mobile Festival, Seville, 2023.
        </p>
        <!-- PENDING: author and publication for proper attribution -->
      </article>
    </div>
    <!-- PENDING: additional reviews and press coverage -->
  </section>
</Base>

<style>
  .press-list {
    margin-top: var(--space-lg);
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .press-item blockquote {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    border-left: 3px solid var(--color-accent-solid);
    padding-left: var(--space-md);
    color: var(--color-text);
    font-style: italic;
    margin-bottom: var(--space-md);
    line-height: 1.4;
  }

  .press-source {
    padding-left: calc(var(--space-md) + 3px);
  }
</style>
